<h1>Olá Handlebars</h1>
<p>Nome do usuário: {{user.name}}, idade: {{user.age}} </p>
<h2>{{frase}}</h2>

{{#if auth}}
    <p>Você está autenticado <a href="/dashboard">Clique aqui</a> para ser redirecionado a dashboard </p>

    {{else}}
    <p>Você não está autenticado, faça login para continuar</p>
{{/if}}